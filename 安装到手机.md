# 好时间 · 装到手机上（安卓 + iPhone 通用）

这是一个 **PWA（渐进式网页应用）**。装好后有独立图标、全屏运行、**可离线使用**，体验接近原生 App，无需上架应用商店。

---

## 方式一：扫码/链接安装（推荐，安卓 + iPhone 都行）

1. 在这台 Mac 上**双击 `start.command`**（首次若打不开：右键 → 打开 → 仍要打开）。
2. 终端里会出现一行网址：`https://xxxxxxxx.lhr.life`
3. 手机连任意网络，用浏览器打开这个网址：
   - **安卓**：用 **Chrome** 打开 → 右上角 `⋮` → **「安装应用 / 添加到主屏幕」** → 确认。桌面上出现「好时间」图标。
   - **iPhone**：用 **Safari** 打开（必须 Safari）→ 底部 **分享按钮 ⬆️** → **「添加到主屏幕」** → 添加。
4. 从桌面图标打开即可。**装好后即便关掉 Mac、断网，App 依然能用**（数据存在手机本地）。

> 这个 `lhr.life` 网址是临时的、每次启动会变。它只在**安装那一次**需要联网打开；装好后 App 已缓存到手机，离线照常用。要重新安装或更新时，再跑一次 `start.command` 拿新网址即可。
> 想要永不变的网址，见下面「方式三：GitHub Pages」。

---

## 方式二：同一 WiFi 局域网（不依赖外网）

1. 双击 `start.command` 启动（或手动 `python3 -m http.server 8000`）。
2. 手机和 Mac 连**同一个 WiFi**。
3. 手机浏览器打开：`http://10.55.17.184:8000`
4. iPhone 可「添加到主屏幕」直接全屏运行；安卓在此方式下因非 HTTPS 只能加快捷方式（离线缓存功能受限）。

---

## 方式三：永久网址（GitHub Pages，免费且稳定）

想要一个**永不变、随时可装**的网址，把这个文件夹传到 GitHub：

```bash
cd /Users/listenzhang/Downloads/haoshijian-app
git init && git add . && git commit -m "好时间 PWA"
# 在 github.com 新建一个仓库 haoshijian，然后：
git branch -M main
git remote add origin https://github.com/<你的用户名>/haoshijian.git
git push -u origin main
```

然后在仓库 **Settings → Pages → Branch 选 main / root → Save**，
几分钟后得到永久网址：`https://<你的用户名>.github.io/haoshijian/`
手机打开它，按方式一的第 3 步安装即可。（需要一个 GitHub 账号）

---

## 文件说明

| 文件 | 作用 |
|---|---|
| `index.html` | App 本体（全部功能） |
| `manifest.webmanifest` | App 名称、图标、全屏配置 |
| `sw.js` | Service Worker，负责离线缓存 |
| `icons/` | App 图标（192/512/苹果触摸图标） |
| `start.command` | 一键启动本地服务 + HTTPS 隧道 |

## 功能（基于经典时间管理书籍）
- 24 小时时间轴 + 记录完整度（《奇特的一生》时间统计法）
- 番茄专注钟（《番茄工作法》），结束自动记一段时间
- 深度工作时长（《深度工作》）
- 艾森豪威尔四象限 + 今日青蛙（《七个习惯》《吃掉那只青蛙》《最重要的事，只有一件》）
- 目标管理、时间价值与成长
